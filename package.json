{
  "name": "chip8-emulator",
  "version": "0.1.0",
  "description": "Chip-8 emulator",
  "scripts": {
    "serve": "python -m http.server 3000",
    "start": "node ./app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JustSong",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.3.0"
  }
}
